# @prodmind/decision-engine

Stateful decision orchestration kernel.

Current state:
- scaffold only
- compile/test scripts wired

Boundary:
- allowed internal dependencies:
  - `@prodmind/shared-types`
  - `@prodmind/llm-adapter`
- no app shell imports

TODO:
- Phase 3 extraction from `prodmind2-cli` and `prodmind2-web` engine modules.

