# @prodmind/app-cli

CLI composition shell.

Current state:
- scaffold only (no migrated business shell yet)

Boundary:
- depends on engines + shared-types + llm-adapter only
- must not re-implement engine business logic

TODO:
- compose unified command surface after engine migration phases.

