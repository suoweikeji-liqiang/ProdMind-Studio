# @prodmind/app-web

Web composition shell.

Current state:
- scaffold only (no migrated UI implementation yet)

Boundary:
- depends on engines + shared-types + llm-adapter only
- must not embed challenge/decision/asset business rules in page shells

TODO:
- wire IA and workflows per `docs/ui-standards.md` during shell composition phase.

