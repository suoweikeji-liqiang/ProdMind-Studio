# Fakes

Shared deterministic fakes live here.

Priority fake to implement next:
- fake LLM provider used by `llm-adapter` and engine integration tests

The fake must support:
- fixed text responses
- fixed structured responses
- injected error cases
- deterministic token streaming

