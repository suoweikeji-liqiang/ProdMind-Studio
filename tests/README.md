# Shared Tests

This directory stores cross-package testing assets and shared test infra.

Subdirectories:
- `fixtures/` static sample inputs
- `golden/` expected outputs that must stay stable
- `fakes/` deterministic fake providers and stubs
- `helpers/` shared test utilities

See `docs/test-layout.md` for placement rules.

